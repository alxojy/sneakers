import { StyleSheet } from 'react-native';

const mainStyles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#fff'
    },
    header: {
        flexDirection: 'row',
        padding: 15
    },
    headerText: {
        fontSize: 25,
        fontWeight: 'bold'
    },
    image: {
        width: '100%'
    }
});

export default mainStyles;